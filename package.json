{
  "name": "agentic-research-site",
  "private": true,
  "scripts": {
    "dev": "npm --prefix site run dev",
    "build": "npm --prefix site run build",
    "preview": "npm --prefix site run preview",
    "research:update": "node research/generate-update.mjs"
  }
}
