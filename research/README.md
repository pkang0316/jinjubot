# Research Pipeline

This directory will hold the scripts and prompts that generate updates for the website.

For now, `generate-update.mjs` writes a placeholder digest into `content/research-digest.json` so the website has a stable contract to build against.
